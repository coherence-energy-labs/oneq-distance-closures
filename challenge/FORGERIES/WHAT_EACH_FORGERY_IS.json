{
  "inflated_lower_bound": "12_6_0201 lower bound inflated 8 -> 10; integrity hashes were regenerated by the attacker, so only the SIGNATURE can refuse it",
  "altered_information_set": "12_6_0199 one pivot replaced with a never-swept qubit -- the signed enumeration plan no longer describes what ran",
  "witness_substitution": "9_6_0172 witness support replaced with a fabricated support -- re-running the witness check must refuse it",
  "downgraded_verifier_floor": "bundled verifier floor lowered 2.0.0 -> 0.0.1 so pre-forgery-fix certificates would pass -- the RELEASE-HASH check must catch the modified verifier",
  "missing_issuer_registry": "ISSUERS.json deleted -- a verifier that claims issuer pinning must REFUSE, never fall back to the key inside the certificate",
  "malformed_issuer_registry": "ISSUERS.json replaced with a wrong-schema, zero-issuer document -- must be refused, not treated as 'no pins configured'",
  "revoked_issuer": "the signing key is now REVOKED in the registry -- certificates it signed must stop being promotable"
}